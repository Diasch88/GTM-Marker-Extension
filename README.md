# GTM-Marker-Extension
Marks the last three clicked elements (tags, triggers, vars) in Google Tag Manager for better overview during structured changes.
Go to extensions section in your browser and activate developer mode. Then load unpacked exxtension from downloaded file.
